# AGENTS.md



## 归属

本文件只拥有 AI 执行行为规则。


## 工作语言

- 默认使用中文回复、解释、总结和提问，除非用户或对应 owner doc 明确要求使用英文。
- 保留代码、命令、文件名、变量名、函数名、类名、包名、配置项的英文形式。
- 技术术语可保留英文；必要时用中文解释。

## 读取顺序

执行任何仓库任务前，按以下顺序建立上下文：

1. 读取本文件，确认 AI 执行约束。
2. 创建、修改或重构任何代码前，完整读取并遵守
   `docs/engineering/utils_catalog.md`。
3. 创建、修改、重构或审查任何 `*.py` 文件时，完整读取并遵守
   `docs/engineering/python_coding_style_for_ai.md`。

### AI 执行约束

#### 文档语言

长期文档默认使用中文编写，除非对应 owner doc 为兼容外部协议、代码生成或第三方接口明确要求英文。

代码、命令、文件名、变量名、函数名、类名、包名、配置项使用英文。

#### Utils 优先复用

- 创建、修改或重构任何代码前，必须完整读取 `docs/engineering/utils_catalog.md`。
- 注册表中存在适用工具时，必须优先复用，不得重复实现等价逻辑。
- 新增、删除、重命名或改变 `src/utils` public 工具的使用范围时，必须同步更新
  `docs/engineering/utils_catalog.md`。

#### Python 编码规则执行

- `docs/engineering/python_coding_style_for_ai.md` 拥有 Python 编码风格规则；本文件只拥有读取和执行这些规则的 AI 行为要求。
- 编码前，依据该文档设计和实现 Python 修改。
- 完成前，依据最终 diff 对该文档中的规则逐项复核；不得只依据任务最初描述确定适用规则。
- 可机械验证的规则必须执行对应检查；无法机械验证的语义规则必须结合代码上下文和对应 owner doc 复核。
- 业务语义、public API、路径、metadata、lineage 和日志边界以对应 owner doc 为准。
- 无法执行某项验证时，必须在完成汇报中说明原因和剩余风险。



## 完成汇报

完成时用中文简要说明：

- 改了什么。
- 更新了哪些 owner docs。
- 复用了哪些 utils；如果新增或变更 utils，说明注册表更新情况。
- 执行了哪些验证。
- 是否存在阻塞或剩余风险。
